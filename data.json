import os

os.chdir(f"/content/{REPO_NAME}")

!git config user.email "colab@auto.push"
!git config user.name "Google Colab Bot"

!git add .
!git commit -m "Auto-update dashboard tekanan"
!git push https://{GITHUB_USERNAME}:{GITHUB_TOKEN}@github.com/{GITHUB_USERNAME}/{REPO_NAME}.git
